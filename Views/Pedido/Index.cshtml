@model List<Pedido>
@{
    ViewData["Title"] = "Lista de pedidos";
}
<section class="container my-5">

    <section class="headerEspaciado col-8">
        <h2>Lista de Pedidos</h2>

        <span>
            <a asp-controller="Pedido" asp-action="Crear">
                <i class="bi bi-plus-circle colorPrimario btn-lg"></i>
            </a>
        </span>
    </section>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Titulo</th>
                                    <th scope="col">Fecha</th>
                                    <th scope="col">Total</th>
                                    <th scope="col">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <form style="display: inline;" asp-controller="Pedido">
                                @{
                                    if(Model != null && Model.Count > 0)
                                        foreach(var p in Model)
                                        {
                                            <tr>
                                                <th scope="row">@p.PedidoId</th>
                                                <td>@p.Titulo</td>
                                                <td>@p.Fecha.ToString("dd/MM/yyyy")</td>
                                                <td>$ @p.Total</td>
                                                <td>
                                                                                                                                                 <!-- Crear `MostrarDetalle` en PedidoController -->
                                                    <a class="btn btn-sm btn-outline-primary me-1" title="Ver detalles" asp-action="MostrarDetalle">
                                                        <i class="bi bi-eye"></i>
                                                    </a>
                                                    <button asp-action="Eliminar" asp-route-id="@p.PedidoId" class="btn btn-sm btn-outline-danger" title="Eliminar" type="submit">
                                                        <i class="bi bi-trash"></i>
                                                    </button>
                                                </td>
                                            </tr>
                                        }
                                    else
                                    {
                                        <tr>
                                            <td colspan="5" class="text-center">
                                                No hay ningún pedido. <a asp-controller="Pedido" asp-action="Crear">Crea uno!</a>
                                            </td>
                                        </tr>
                                    }
                                }
                                </form>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>